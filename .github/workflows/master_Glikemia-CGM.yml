# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy container app to Azure Web App - Glikemia-CGM

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read #This is required for actions/checkout

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to container registry
        uses: docker/login-action@v2
        with:
          registry: mcr.microsoft.com/
          username: ${{ secrets.AzureAppService_ContainerUsername_7a29173813044caaa2277dcaed7aaa4a }}
          password: ${{ secrets.AzureAppService_ContainerPassword_0a6b9ca7bee945c3a1fb372730cd96fd }}

      - name: Build and push container image to registry
        uses: docker/build-push-action@v3
        with:
          context: .
          push: true
          tags: mcr.microsoft.com/${{ secrets.AzureAppService_ContainerUsername_7a29173813044caaa2277dcaed7aaa4a }}/appsvc/staticsite:${{ github.sha }}
          file: ./Dockerfile

  deploy:
    runs-on: ubuntu-latest
    
    needs: build
    
    steps:
      
      - name: Deploy to Azure Web App
        id: deploy-to-webapp
        uses: azure/webapps-deploy@v2
        with:
          app-name: 'Glikemia-CGM'
          slot-name: 'Production'
          images: 'mcr.microsoft.com/${{ secrets.AzureAppService_ContainerUsername_7a29173813044caaa2277dcaed7aaa4a }}/appsvc/staticsite:${{ github.sha }}'
          publish-profile: ${{ secrets.__publishingprofilesecretname__ }}
